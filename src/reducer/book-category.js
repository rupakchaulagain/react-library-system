import bookCategory from "../data/book-category";

export default (state = bookCategory, action) => {

    switch (action.type()) {

        case '':

        case '':

        default:

    }
    return state;
}